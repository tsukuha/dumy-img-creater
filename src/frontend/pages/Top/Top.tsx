import * as React from 'react';
import { TimeLine } from './components/TimeLine';

const Top = (): JSX.Element => {
    return (
        <div>
            <TimeLine />
        </div>
    );
};

export { Top };
